import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: '100%',
        borderWidth: 1,
        borderColor: '#D9D9D9',
        padding: 14,
        borderRadius: 8,
        marginBottom: 16,
        fontSize: 16,
    }
});